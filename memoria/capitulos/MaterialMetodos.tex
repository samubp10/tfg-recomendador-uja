% !TEX root = ../main.tex
\chapter{MATERIAL Y MÉTODOS}
\label{cap:materialesymetodos}

Este capítulo traduce los objetivos formulados en el Capítulo~\ref{cap:objetivos} en decisiones técnicas concretas y en su justificación. Se organiza en torno a la metodología de trabajo seguida, la planificación temporal, las tecnologías y herramientas empleadas, la arquitectura general del sistema, el diseño de los datos y, por último, el desarrollo de la Fase 0. Toda decisión técnica relevante se presenta con el mismo patrón: las alternativas consideradas, la evidencia que las distingue y la elección justificada, dejando las decisiones de arquitectura más importantes documentadas además en su registro (\gl{ADR}) correspondiente, en el formato breve que propuso Nygard\cite{nygard2011adr}: contexto, alternativas, decisión y consecuencias. Las anomalías de calidad de datos encontradas en la fuente siguen el mismo principio de evidencia, pero se recogen en un registro distinto (\gl{DQA}), porque ninguna de ellas es, por separado, una decisión de arquitectura.



\section{Metodología de trabajo}

Dada la naturaleza exploratoria y de investigación de este Trabajo Fin de Grado, se ha descartado el uso de metodologías tradicionales en cascada o de marcos ágiles rígidos diseñados para equipos grandes (como Scrum), cuyo sobrecoste de gestión resultaría ineficiente para un único desarrollador. En su lugar, se ha adoptado una metodología ágil basada en \textbf{\gl{Kanban}}\cite{anderson2010kanban}, priorizando la flexibilidad de las tareas, pudiendo eliminar, añadir o modificarlas, la entrega continua de valor y la adaptación del código ante los descubrimientos realizados sobre los datos de la fuente y los cambios de la misma.

Además de esta metodología para la gestión de tareas, el proyecto se apoya en cuatro pilares de ingeniería de \textit{software}: gestión visual del trabajo, control de versiones trazable, \sig{integración continua} y un estándar de calidad.

\subsection{Gestión del trabajo y priorización}

El ciclo de vida del proyecto se ha gestionado mediante un tablero \textbf{Kanban} virtual (implementado en \textit{GitHub Projects}), dividiendo el alcance total en unidades de trabajo atómicas o historias de usuario. Estas tareas se agrupan secuencialmente en seis fases lógicas (Fase 0 a Fase 5), desde la minería de datos hasta el cierre y la preparación de la defensa.

Para gestionar la priorización de las tareas, se ha aplicado la técnica de \textbf{MoSCoW} \cite{moscow} (\textit{Must have, Should have, Could have, Won't have}), asegurando que los componentes críticos del sistema RAG se desarrollen de manera prioritaria frente a las características opcionales.

\subsection{Estrategia de control de versiones y trazabilidad}
Para aislar los entornos de desarrollo y proteger la integridad del sistema, se ha definido una estrategia estricta de ramas (\textit{Git Flow}):
\begin{itemize}                                                                                                                                                    
  \item \textbf{Ramas permanentes:} Se mantiene una rama \texttt{main} que aloja de forma exclusiva el código estable y funcional, y una rama paralela \texttt{doc} dedicada íntegramente a la redacción iterativa de la presente memoria en \LaTeX.
  \item \textbf{Ramas volátiles:} Cada nueva característica, corrección o experimento se desarrolla en una rama aislada de un solo uso (ej. \texttt{IT-XX-descripcion}).
        
        La Figura~\ref{fig:git_flow} recoge esta estrategia: cada rama volátil (que se borrará después de terminar el IT) nace siempre de \texttt{main} actualizado y vuelve a él mediante una confirmación de fusión, de modo que la rama \texttt{doc} avanza en paralelo sin bloquear el desarrollo del código.
        
        \begin{figure}[H]
          \centering
          \includegraphics[width=0.8\textwidth]{imagenes/GitFlow.pdf}
          \caption{Estrategia de ramificación y versionado empleada en el desarrollo. Fuente: Elaboración propia.}
          \label{fig:git_flow}
        \end{figure}
        
\end{itemize}


Para garantizar la trazabilidad de las decisiones de diseño a lo largo del tiempo, se ha adoptado el estándar \textit{Conventional Commits} \cite{conventionalcommits}. Esta convención impone una estructura estandarizada para cada entrega de código, facilitando la lectura semántica del historial del proyecto. El formato base utilizado es \texttt{tipo(item de trabajo): descripción}, donde el \textit{item de trabajo} corresponde al identificador de la historia técnica en curso (ej. \texttt{IT-05}).

Para clasificar semánticamente los cambios, los tipos de confirmación (\textit{commits}) se han restringido a la siguiente taxonomía:
\begin{itemize}                                                                                                                                                    
  \item \textbf{\texttt{feat}:} Incorporación de una nueva funcionalidad al sistema (ej. implementación de un nuevo módulo de extracción o lógica de procesamiento).
  \item \textbf{\texttt{fix}:} Corrección de errores o fallos detectados en el código existente.
  \item \textbf{\texttt{docs}:} Creación o actualización de documentación, incluyendo la redacción de la presente memoria y el registro de decisiones arquitectónicas (ADR).
  \item \textbf{\texttt{test}:} Adición o corrección de pruebas automatizadas, asegurando la no regresión sin alterar el código de producción.
  \item \textbf{\texttt{refactor}:} Reestructuración interna del código que mejora su legibilidad, diseño o rendimiento, sin añadir nuevas funcionalidades ni corregir errores externos.
  \item \textbf{\texttt{chore}:} Tareas de mantenimiento, actualización de dependencias o configuración de las herramientas de integración continua.
\end{itemize}

Cada entrega de código exige no solo un título estandarizado, sino un cuerpo explicativo que justifique la decisión técnica frente a sus alternativas. Para preservar intacta esta trazabilidad y el razonamiento, la integración en la rama principal (\texttt{main}) se realiza mediante confirmaciones explícitas (\textit{merge commits}), descartando el aplastamiento del historial (\textit{squash}).

\subsection{Integración Continua (CI) y automatización de pruebas}
\label{secc:Integracion_continua}
El aseguramiento del correcto funcionamiento del \textit{software} se apoya en un sistema de Integración Continua (\sig{CI}) desplegado a través de \textit{GitHub Actions}. Este sistema intercepta cada actualización del repositorio y ejecuta automáticamente una batería de pruebas unitarias implementadas con \texttt{pytest}\cite{pytest}.

Un principio de esta metodología ha sido la validación contra la realidad. Las pruebas de los módulos de extracción web (\glr{spiders}{spider}) no se basan en datos inventados o simulados, sino en conjuntos de datos de prueba predefinidos, estáticos y reales en PDF descargados de la web de la Escuela Politécnica Superior de Jaén. Esto garantiza que las pruebas verifiquen el comportamiento del código ante las verdaderas anomalías de la fuente.

Cabe destacar que, debido al alcance académico del proyecto y a la ausencia de un entorno de producción al público (una página web subida a un \textit{hosting}), el enfoque de este trabajo se limita estrictamente a la Integración Continua (CI), excluyendo las prácticas de Despliegue Continuo (CD), lo que permite concentrar los recursos en la experimentación y métricas de evaluación del modelo de lenguaje.

Junto a las pruebas funcionales, la integración continua automatiza la verificación del estilo y la calidad del código. La documentación de las funciones sigue el estándar PEP~257\cite{pep257} y la guía de estilo de Google\cite{googlestyle}, estructurando explícitamente los argumentos y las variables de entorno. Además, el formato del código se estandariza mediante \texttt{flake8}, y la comprobación de tipos se delega en \texttt{mypy}.

\subsection{Tipos de pruebas del proyecto}
\label{secc:niveles_de_prueba}

En este trabajo conviven tres tipos de pruebas que responden a preguntas distintas, se ejecutan
en momentos distintos y, sobre todo, no significan lo mismo cuando fallan. Estos tipos son de vital importancia
para asegurar que todas las partes del sistema funcionan correctamente y que los resultados son fidedignos.

\textbf{El primer tipo son las pruebas del código}, escritas con
\texttt{pytest} y ejecutadas antes de la ejecución del código que comprueban y cuando se suben al repositorio (explicado en la sección ~\ref{secc:Integracion_continua}). Comprueban que cada módulo o script hace lo que está previsto que haga, son deterministas y su resultado es de carácter booleano, es decir, pasan o no pasan. Cuando fallan, el defecto está en el código.

\textbf{El segundo tipo son los verificadores de las colecciones de datos}, que no examinan el código sino el resultado de ejecutarlo sobre los ficheros que contienen toda la información, como por ejemplo el que contiene todos los datos de la recolección web y el fichero que contiene los chunks de los anteriores. Comprueban que no hay anomalías en los datos, como por ejemplo que no falte ninguna titulación, si algún fragmento del fichero de los chunks supera el tamaño máximo o si el fragmento nombra a una unidad  (Asignatura, plan de estudio o Salida profesional) distinta a la suya.

Este tipo de pruebas se ejecutan a mano antes de cada subida al repositorio y no en la integración continua debido a que la colección de datos no se versiona ya que son ficheros grandes y con previsión a ser aún más grandes en el futuro (Sección~\ref{secc:verificacion_fase0}).

\textbf{El tercer tipo es la evaluación del sistema}, la cual más que una prueba es una medición cuantitativa. No produce un resultado booleano como en las pruebas anteriores, sino
uno o varios números que solo tienen significado si se comparan con el de otra configuración.

Sobre la recuperación de fragmentos, se utilizan las métricas de \textit{Recall@K} y \textit{MRR} contra un conjunto de preguntas etiquetadas a mano, escritas pensando en lo que el usuario pudiera querer saber.
sobre la generación de la respuesta se medirán la fidelidad de la respuesta al contexto
recuperado y su pertinencia.

Esta actividad no puede automatizarse en la integración continua por dos motivos: necesita descargar modelos para la experimentación y comprobación de cuál es mejor (de varios cientos de megabytes) y su
resultado no es un fallo que arreglar, sino una comparación que interpretar.
Los resultados se recogen en el Capítulo~\ref{cap:resultados}.




\subsection{Criterios de finalización (Definición de Hecho)}
\label{secc:dod}
Para evitar la ambigüedad sobre el estado de avance del TFG, se ha establecido una estricta \textbf{Definición de Hecho} (\textit{Definition of Done}). Una tarea (cada una de las tarjetas con identificador empezando por IT-XX) solo se considera finalizada y se fusiona en el sistema si cumple las definiciones, dependiendo del tipo que sea la tarjeta. Siguiendo esta lógica, se han dividido estos criterios en dos grupos, los que valen para todas las tarjetas, y los que dependen del tipo de tarea al que va enfocada la tarjeta (Tabla~\ref{tab:resumen_backlog}).

Los tres criterios que valen para todas las tarjetas:

\begin{enumerate}                                                                                  
  \item \textbf{Defendibilidad:} la decisión que hay detrás se puede explicar, con la evidencia delante y diciendo qué alternativas se descartaron.
  \item \textbf{Memoria:} lo que la tarjeta ha producido está volcado en la sección que le corresponde de este documento.
  \item \textbf{Integración:} el trabajo está fusionado en la rama principal y la integración continua está en verde.
\end{enumerate}

Los otros cinco tipos de tarjetas con sus criterios de finalización son:

\begin{itemize}                                                                                                                  
  \item \textbf{Código.} El programa hace lo que promete, comprobado
        \emph{ejecutándolo} y con las pruebas automatizadas construidas sobre los datos reales de la fuente
        , incluida la de regresión de cualquier defecto que se haya encontrado.
  \item \textbf{Experimento.} El guion que produce las cifras está
        versionado y es determinista, de modo que otra persona pueda repetirlo y obtener lo
        mismo, y este además se encuentra en la memoria y en un documento técnico.
  \item \textbf{Decisión.} Existe el registro correspondiente de arquitectura o de calidad de datos con las alternativas consideradas, la evidencia
        que sostiene la elegida y sus limitaciones.
  \item \textbf{Memoria.} El documento compila, no queda texto de
        borrador de la sección sin reescribir en la parte afectada, y toda cifra que se dé procede de la
        salida de un experimento o un verificador, además de
        respetar la estructura y el estilo que fija la Escuela para los Trabajos Fin
        de Grado\cite{epsj_criterios}.
  \item \textbf{Gestión.} Todos los cambios en el repositorio se comprueban a posteriori que no han roto nada.
\end{itemize}

\section{Planificación temporal}

Dado el enfoque ágil y experimental de este Trabajo Fin de Grado, la planificación temporal huye de las estimaciones predictivas rígidas tradicionales (como el modelo en cascada). Un sistema RAG requiere iteración y ajuste continuo ante los datos descubiertos, por lo que una planificación lineal resultaría irreal. En su lugar, se ha optado por una estrategia de planificación a dos niveles: una macro-planificación orientada a hitos de entrega y una micro-planificación diaria basada en flujo continuo.

\subsection{Micro-planificación: Tablero Kanban}

La ejecución del día a día se rige por un tablero \textbf{Kanban} implementado mediante \textit{GitHub Projects}. Este tablero refleja el estado en tiempo real de las  historias técnicas de usuario que componen el \textit{backlog} del proyecto.

Como se observa en la Figura \ref{fig:tablero_kanban}, el flujo de trabajo se divide en columnas que representan el ciclo de vida de cada tarea. La regla principal de esta micro-planificación es el límite de Trabajo en Curso (\glr{Work In Progress}{WIP}, WIP), que impide iniciar nuevas tareas sin haber cerrado las anteriores. Esto garantiza un avance constante, previniendo el estancamiento y asegurando el cumplimiento estricto de la Definición de Hecho expuesta en la sección anterior.

\begin{figure}[H]
  \centering
  \includegraphics[width=1 \textwidth]{imagenes/tableroKanBan.png}
  \caption{Captura del tablero Kanban utilizado para la gestión ágil del proyecto en GitHub Projects. Fuente: Elaboración propia.}
  \label{fig:tablero_kanban}
\end{figure}

\subsection{Macro-planificación: Hitos y fases (Gantt)}

Para proporcionar una visión global del cronograma y asegurar la viabilidad del proyecto de cara a la fecha de entrega del proyecto, el \textit{backlog} se ha agrupado en seis fases lógicas de desarrollo.

Esta macro-planificación iterativa se ilustra en el diagrama de Gantt de la Figura \ref{fig:gantt}, estructurado de la siguiente manera:
\begin{itemize}                                                                                                                                                    
  \item \textbf{Fase 0 (Minería de datos y extracción):} Abarca el desarrollo de los rastreadores web (\textit{spiders} en Scrapy), los validadores lógicos y los módulos de limpieza de texto.
        
  \item \textbf{Fase 1 (Troceado e Incrustaciones):} Comprende la experimentación para la selección del algoritmo de troceado y la experimentación para la elección del modelo de \textit{embeddings}.
        
  \item \textbf{Fase 2 (Base de datos vectorial, Pipeline RAG y LLM):} Comprende la indexación de los fragmentos en la base de datos vectorial, el diseño del \textit{prompt} del sistema, la selección e integración del modelo de lenguaje grande y la evaluación cuantitativa exhaustiva de las respuestas generadas.
        
  \item \textbf{Fase 3 (Aplicación web de chat):} Diseño de la arquitectura e implementación de la interfaz conversacional accesible (sin registro) y su conexión con el \textit{backend}.
        
  \item \textbf{Fase 4 (Validación experimental):} Estudios de ablación sobre los
        parámetros del recuperador y evaluación del sistema completo sobre el banco de
        preguntas. Esta fase incluía además pruebas con estudiantes de perfil
        preuniversitario, que quedaron fuera del alcance por las razones que expone el
        Apartado~\ref{secc:validacion_usuarios}.
        
  \item \textbf{Fase 5 (Cierre y memoria):} Volcado final de documentación técnica, revisión de los resultados experimentales y preparación del material de defensa oral (Aunque la memoria se está haciendo a la par que el desarrollo).
        
\end{itemize}

\input{diagramas/DiagramaGantt.tex}


\section{Priorización y estimación del esfuerzo}

El \textit{backlog} del proyecto está formado por historias de usuario. Para decidir en qué orden abordarlas se han
combinado dos criterios independientes: cuánto valor aporta cada una al resultado
final y cuánto esfuerzo cuesta.

\subsection{Priorización por valor: MoSCoW}
\label{secc:moscow}

El valor se ha clasificado con la técnica \textbf{MoSCoW}~\cite{moscow}, que
divide las tareas en cuatro categorías según su necesidad real para el
proyecto:

\begin{itemize}                                                                                                                                                    
  \item \textbf{\textit{Must have} (imprescindible):} sin esta tarea el
        sistema no funciona o la memoria no es defendible. Aquí están el rastreo de la
        web, la fragmentación de los textos, la elección experimental de los
        modelos y la evaluación cuantitativa del sistema.
  \item \textbf{\textit{Should have} (importante):} aporta valor claro y se
        hará salvo que el tiempo lo impida, pero su ausencia no invalida el
        resultado.
  \item \textbf{\textit{Could have} (deseable):} mejora el trabajo si sobra
        tiempo.
  \item \textbf{\textit{Won't have} (descartado):} queda explícitamente fuera
        del alcance, como el despliegue en producción o la extensión al resto de
        facultades de la Universidad de Jaén.
\end{itemize}

Esta clasificación tiene una consecuencia práctica: ante un retraso, se sacrifica
primero lo \textit{could} y después lo \textit{should}, pero nunca lo
\textit{must}. Este reparto no es una foto fija:
el \textit{backlog} es un artefacto vivo y ha crecido durante el desarrollo.


\subsection{Estimación del esfuerzo: tallas de camiseta}

Para estimar el coste se ha empleado la técnica de las \textbf{tallas de
  camiseta} (\textit{t-shirt sizing}), que asigna a cada historia una talla
relativa en lugar de una cifra de horas. Las estimaciones en horas dan una falsa sensación de precisión y,
en un trabajo con una fuerte componente de investigación (donde una tarde puede
irse en descubrir que la web codifica mal sus propias páginas), resultan poco
fiables y variables. Comparar tamaños relativos es más honesto y, sobre todo, más útil.

Las tallas empleadas son:

\begin{itemize}                                                                                                                                                    
  \item \textbf{XS:} tarea mecánica, sin decisiones de diseño (por ejemplo,
        generar un diagrama a partir del código).
  \item \textbf{S:} una sola unidad de trabajo bien acotada, resoluble en una
        sesión (un script pequeño con sus pruebas, o un registro de decisión).
  \item \textbf{M:} exige varias sesiones o tomar decisiones de diseño no
        triviales (una araña que debe lidiar con anomalías de la fuente).
  \item \textbf{L:} implica experimentación con resultados abiertos: no se
        sabe de antemano cuánto costará porque depende de lo que muestren los datos
        (la comparativa de modelos de incrustaciones o la evaluación del sistema
        completo).
\end{itemize}

El cruce de ambos criterios (MoSCoW y tallas de camisetas) es lo que determina el orden de trabajo, una historia
\textit{must} de talla L (como la evaluación cuantitativa) se planifica con
antelación y se le reserva más tiempo, mientras que una \textit{could} de talla L
sería la primera candidata a quedar fuera.

\subsection{Relación de historias de usuario}

Cada historia se identifica con un código IT-XX que se usa en todo el proyecto: da nombre a la rama de trabajo, aparece en el ámbito de cada confirmación de código (\texttt{feat(IT-05): \ldots}) y etiqueta
la incidencia correspondiente en el repositorio, de modo que cualquier línea de código puede rastrearse hasta la necesidad que la originó.

A lo largo del proyecto se han creado muchas historias de usuario de diferentes tipos, dificultad y duración y desarrollar todas y cada una de ellas aquí lo único que haría sería introducir datos e información poco útil y poco necesaria a este proyecto de investigación. Es por ello por lo que se ha recogido en la Tabla~\ref{tab:resumen_backlog} cuántas tarjetas hay en cada fase del proyecto y de qué tipo. El detalle de cada historia, así como su descripción, el historial de commits y otra información relevante queda accesible en el gestor de incidencias del repositorio \cite{repo_tfg}.


\input{tablas/TablaResumenBacklog.tex}


\section{Tecnologías y herramientas}

Antes de la descripción de la construcción del sistema, resulta necesario establecer dos premisas que determinan y enmarcan las decisiones técnicas posteriores. La primera concierne al enfoque arquitectónico, que consiste en establecer una estrategia para garantizar que el modelo de lenguaje genere respuestas veraces y no alucine, justificando el descarte de técnicas de reentrenamiento. La segunda premisa define las restricciones \textit{hardware} del equipo sobre el que se ejecuta el sistema. Esta limitación de memoria constituye un cuello de botella que condiciona de forma directa la selección posterior del modelo de incrustaciones, la base de datos vectorial y el modelo generativo.

\subsection{RAG frente al ajuste fino}

Una alternativa sería ajustar los parámetros del modelo con información específica del dominio mediante ajuste fino (\textit{fine-tuning}). Este procedimiento puede resultar adecuado para adaptar el comportamiento, el formato de salida o el desempeño del modelo en una tarea concreta, pero no ofrece por sí solo una referencia explícita a la fuente de cada afirmación. Además, cuando los datos cambian con frecuencia, mantener el conocimiento actualizado exigiría preparar nuevos ejemplos y repetir el proceso de ajuste, lo que resultaría en una alta carga computacional cada vez.

En el contexto de este trabajo, la arquitectura RAG permite separar el modelo de lenguaje de la información oficial de la EPSJ, haciendo de ambos dos módulos independientes e intercambiables. Los planes de estudio, las guías docentes y las salidas profesionales pueden actualizarse en la colección sin modificar los parámetros del modelo, a la vez que los fragmentos recuperados proporcionan una base documental para las respuestas. No obstante, la calidad final dependerá de todo el proceso (\textit{pipeline} RAG), la cobertura y limpieza de la colección, la estrategia de fragmentación, la capacidad del recuperador para seleccionar información pertinente y las instrucciones proporcionadas al modelo durante la generación.


\subsection{Equipo de ejecución}
\label{secc:equipo_ejecucion}

Todo el sistema (extracción, fragmentación, indexación, recuperación
y generación) ha sido probado y ejecutado sobre un único equipo portátil doméstico, cuyas
características recoge la Tabla~\ref{tab:equipo}. Este equipo no es un detalle de implementación, sino que es una restricción de diseño que
aparece nombrada en la hipótesis de trabajo (Sección~\ref{secc:hipotesis}) y que ha condicionado las decisiones tomadas al no poder contar con un equipo más potente que hubiera permitido emplear modelos mejores y más grandes.

El recurso que más pesa hoy en día en inteligencia artificial es, sin lugar a dudas, la memoria principal, y es el mayor limitante de este equipo: en ella tienen que convivir el modelo de incrustaciones, el modelo de lenguaje generativo con su contexto y la base de datos vectorial con las incrustaciones de la colección, y esa suma es la que impide elegir modelos de más prestaciones.

\input{tablas/TablaEquipo.tex}


\section{Arquitectura del sistema}

Como se observa en la Figura~\ref{fig:arquitectura_pipeline}, primero se obtienen los datos necesarios para las recomendaciones (grados, asignaturas, guías y salidas laborales). Antes de fragmentarse, ese texto pasa por el módulo de limpieza, que corrige la codificación y repara las \sig{URL} rotas de la fuente directamente extraída de la página web oficial. Este paso no aparece como caja propia en la Figura~\ref{fig:arquitectura_pipeline} porque se ejecuta dentro del propio rastreador, sobre cada ítem según se extrae, y no como una etapa posterior independiente. A continuación, el texto se pasa por el fragmentador (\textit{chunker}), que parte este en fragmentos manejables para el modelo de incrustaciones, según se detalla en la Sección~\ref{subsec:chunking}.

\textcolor{red}{No se comienzan los párrafos con 'Y'}

\textcolor{blue}{[sbp] Corregido, y revisado el resto del documento para que no vuelva a ocurrir.}

Con estos trozos de información, se pasa al \textit{embedding} con el que se introducirán los datos en la base de datos vectorial.

%OJO te faltaba una "d" en embedding. 

Por último llega el \textit{pipeline} RAG: tras la pregunta del usuario, el sistema, utilizando como memoria la base de datos vectorial, recupera los fragmentos pertinentes a la pregunta y responde. Con toda esta última fase se podrá interactuar mediante la aplicación web.

Cabe recalcar que el rastreador y el fragmentador son dos módulos independientes: el primero no sabe nada de cómo se va a fragmentar el texto que extrae, y el segundo no sabe nada de cómo se obtuvo el texto que recibe, solo que le llega el fichero que contiene todos los datos en crudo. Esta separación es intencionada: volver a fragmentar es una operación barata que se repite muchas veces durante la experimentación, mientras que volver a rastrear la web es costoso y multiplica las peticiones al servidor de la universidad. El razonamiento completo, con la evidencia que lo sostiene, se recoge en la Sección~\ref{secc:verificacion_fase0} y en el ADR-0001.

\textcolor{red}{La imagen es poco legible, el texto es muy pequeño}

\textcolor{blue}{[sbp] Corregido: he ampliado la figura del 50\,\% al 90\,\% del ancho de página. Si no es lo suficientemente legible igual debería escalar la fuente.}



\input{diagramas/DiagramaArquitecturaPipeline.tex}


\section{Diseño de los datos}


El diagrama de clases de la Figura~\ref{fig:modelo_datos} recoge las diferentes estructuras de datos de la aplicación, que pueden dividirse en dos partes claras: la primera es la de los datos en crudo extraídos de la web, como la relacionada con los grados y las asignaturas, cada cual con su guía docente (si está disponible) y sus salidas. Toda esta información converge y se trocea para dar lugar a los fragmentos (\textit{chunks}), que además contienen un entero que indica en cuántos trozos totales se ha dividido la unidad original (asignatura, salida o guía) y el índice del trozo de información.

\input{diagramas/DiagramaModeloDatos.tex}



\section{Fase 0: construcción de la colección documental}

\subsection{Situación de partida}
\label{secc:fase0_situacion}

Para crear la arquitectura RAG propuesta, el requisito indispensable es disponer de la información oficial de manera precisa y actualizada. La EPSJ publica de forma abierta todos estos datos relativos a sus planes de estudio en su portal web oficial\cite{epsj_grados}. No obstante, un análisis técnico a nivel de código (HTML en el catálogo de titulaciones, PDF en las guías docentes) preliminar de estas fuentes revela deficiencias estructurales que imposibilitan la búsqueda automatizada directa por parte de la IA.

Durante el estudio de la fuente de datos, se han documentado los siguientes obstáculos técnicos:

\begin{itemize}
  \item \textbf{Inconsistencia en la estructura de navegación:} Las rutas (URLs) de acceso a las distintas titulaciones presentan patrones irregulares y \glr{slugs}{slug} inconsistentes en el cuerpo de las páginas.
  \item \textbf{Tablas de datos híbridas y heterogéneas:} La relación de asignaturas no sigue un esquema tabular único. Se combinan listas de materias troncales con tablas de optativas agrupadas por mención, lo que provoca duplicidades en la fuente. Además, no existe una nomenclatura estandarizada entre planes (por ejemplo, el Trabajo Fin de Grado recibe clasificaciones distintas según la antigüedad de la titulación).
  \item \textbf{Ruido tipográfico y errores nativos de enlace:} En el código fuente se ha detectado la presencia de caracteres invisibles (espacios de no separación o de ancho cero) e iconos incrustados en el texto. Asimismo, la plataforma presenta errores de generación nativos, tales como URLs de guías docentes con sufijos duplicados (ej. \texttt{\dots\_es.htmles.html}) que derivan en enlaces rotos.
  \item \textbf{Datos incompletos y registros de relleno:} En titulaciones de reciente implantación (como el Grado en Inteligencia Artificial y Ciberseguridad), materias de cursos superiores carecen aún de guía docente publicada. Paralelamente, en las tablas de matriculación aparecen filas de relleno con nombres genéricos (ej. <<Optativa 1>>), sin código oficial, que no representan asignaturas reales.
  \item \textbf{Cambio de formato de la fuente a mitad de curso:} los enlaces a las guías docentes del corpus cambiaron de formato para este curso lectivo, sirviendo ahora únicamente PDF, pero bajo URLs que conservan la extensión \texttt{.html}. Al no existir correspondencia entre la extensión del enlace y el recurso real, la naturaleza del archivo solo puede determinarse examinando la cabecera HTTP \texttt{Content-Type} (o la firma binaria del documento). 
\end{itemize}

Cada una de estas anomalías, con la evidencia real que la respalda y el tratamiento aplicado en el código, se documenta en los cinco registros DQA\cite{repo_tfg}, aquí se recogen solo a alto nivel para no desviar la memoria hacia el detalle de problemas puntuales.



\subsection{Justificación del enfoque técnico}

Por todo esto, se decidió desarrollar el primer bloque de este proyecto: la implementación de un motor de extracción personalizado (\textit{web scraper}) y de limpieza de texto.

Alimentar un sistema RAG con datos ruidosos o enlaces rotos desencadenaría el antipatrón \textit{Garbage In, Garbage Out} (basura entra, basura sale). Por tanto, es obligatorio desplegar una fase inicial de recolección de datos que actúe como filtro determinista. Solo mediante la validación de filas, la limpieza de caracteres anómalos y la reparación de enlaces rotos, se puede asegurar que la base de datos vectorial sobre la que operará la Inteligencia Artificial contenga una fuente de conocimiento íntegro y de alta calidad.


\subsection[Elección de la herramienta de extracción]{Elección de la herramienta de extracción}
\label{sec:eleccion_herramientas}

Como lenguaje base se ha utilizado Python en su versión 3.13 y, para la extracción de datos, se han estudiado tres claras opciones, las cuales se comentan en los apartados siguientes.

\subsubsection{Scrapy\cite{scrapy}}

Scrapy\footnote{\url{https://scrapy.org/}} es un \textit{framework} asíncrono completo de \textit{crawling} (rastreo web). Dadas sus características, las cuales se detallan a continuación, ha sido la opción elegida.

\begin{itemize}                                                                                                                                                    
  \item \textbf{Codificación de textos:} Los objetos \texttt{TextResponse} y \texttt{HtmlResponse} resuelven la codificación automáticamente siguiendo este orden de prioridad:
        \begin{enumerate}                                                                                                                                                    
          \item Codificación indicada explícitamente al construir la respuesta.
          \item BOM (\textit{Byte Order Mark}) al principio del cuerpo.
          \item Cabecera HTTP \texttt{Content-Type}.
          \item Declaración de codificación dentro del propio documento (etiqueta \texttt{<meta>}).
          \item Autodetección por parte del \textit{framework}.
        \end{enumerate}
        Las guías docentes de la UJA no llevan BOM, por lo que decide la cabecera HTTP: al anteponerla a la etiqueta \texttt{<meta>} del cuerpo, Scrapy decodifica correctamente unas guías servidas en ISO-8859-1 pese a que se declaren como UTF-8, resolviendo el problema de codificación sin necesidad de código adicional.
        
  \item \textbf{Respeto a robots.txt:} Soporte nativo mediante \texttt{ROBOTSTXT\_OBEY = True}, que permite cumplir automáticamente con las directrices de rastreo del sitio.
        
  \item \textbf{Control de tasa (\textit{throttling}):} El \textit{framework} ofrece parámetros de configuración que permiten ajustar el proceso de rastreo para no saturar el servidor objetivo.
        
  \item \textbf{Pruebas sin conexión:} Permite simular respuestas HTML (con cuerpo, URL y cabeceras HTTP) directamente en memoria. Esto facilita la creación de pruebas unitarias sin necesidad de realizar peticiones de red reales.
        
  \item \textbf{Madurez y soporte:} Es un \textit{framework} de referencia en la industria, mantenido activamente. Además, desde la serie 2.13, incluye soporte de tipado estático nativo (\texttt{py.typed}) conforme al estándar PEP 561.\cite{scrapydocs}
        
  \item \textbf{Desventaja:} Presenta una curva de aprendizaje inicial más pronunciada debido a su complejidad y al gran número de componentes integrados.
\end{itemize}


\subsubsection{BeautifulSoup\cite{beautifulsoup}}

Combina un \textit{parser} de HTML (BeautifulSoup) con un cliente HTTP (\texttt{requests}).

\begin{itemize}                                                                                                                                                    
  \item \textbf{Pros:} Gran simplicidad, ideal para tareas pequeñas y estáticas.
  \item \textbf{Contras:} BeautifulSoup no es un rastreador web (no gestiona colas, reintentos ni \textit{rate limiting}); \texttt{requests} no respeta \texttt{robots.txt} automáticamente; el manejo de la codificación (\textit{encoding}) queda completamente en manos del desarrollador. Se considera una herramienta demasiado simple para los requisitos del proyecto.
\end{itemize}

\subsubsection{Selenium \cite{selenium} / Playwright}
Se basa en la automatización de navegadores con renderizado real.

\begin{itemize}                                                                                                                                                    
  \item \textbf{Pros:} Imprescindibles para Single Page Applications (SPAs) y contenido cargado dinámicamente mediante JavaScript.
  \item \textbf{Contras:} La web de la EPSJ está desarrollada en Drupal con contenido estático. Por tanto, el renderizado de JavaScript es innecesario, lo que haría que el programa fuera más lento y consumiera más recursos de los necesarios.
\end{itemize}

%[jccuevas] \paragraph{Decisión final}
\paragraph{Decisión final}
Conforme a lo comentado anteriormente, se adopta \textbf{Scrapy} como herramienta de extracción de datos de webs. La evidencia sobre la gestión correcta de la codificación fue el factor decisivo, reforzado por el soporte nativo de \texttt{robots.txt}, el control de tasa configurable, la testabilidad sin conexión con \texttt{HtmlResponse} y la gran comunidad de soporte. Las alternativas analizadas requerían reimplementar manualmente dichas garantías o estaban sobredimensionadas para un sitio estático.

\subsection{Extracción del catálogo de titulaciones y asignaturas}


En la Figura~\ref{fig:secuencia_scraping} se detalla la secuencia de peticiones entre el
rastreador (\textit{spider}) y la página web.

El rastreador no hace una única petición, sino que encadena varias, una por cada tipo de página de la web, primero el listado de titulaciones, siguiendo siempre el enlace de cada grado, después la portada de cada grado, donde se identifica si es un doble grado por el nombre, a continuación la tabla de asignaturas, y por cada asignatura con enlace propio, su guía docente.



\textcolor{red}{[jccuevas] Igual que antes un poco pequeño el texto. No tengas miedo de usar más espacio para que el texto sea legible.}

\textcolor{blue}{[sbp] Corregido}


\input{diagramas/DiagramaScraping.tex}

\subsection{Extracción de las guías docentes}

La extracción de las guías docentes es la etapa donde se concentran las anomalías en el contenido aportado por el servidor.

En el curso 2026/2027, la Escuela Politécnica Superior de Jaén ofrece la completitud de las guías docentes de las asignaturas en formato PDF, una nueva medida introducida para este curso puesto que anteriormente se servía en formato HTML.

No todas las asignaturas disponen de guía publicada: de las 528 asignaturas del catálogo, 442 cuentan con guía docente con
  contenido y 86 no la tienen todavía, lo que supone una cobertura del 83,7\,\%.
Estas se concentran en las titulaciones de implantación reciente, cuyos cursos superiores aún no
han publicado sus guías, como ocurre en el Grado en Inteligencia Artificial y Ciberseguridad.

La figura que viene a continuación ~\ref{fig:anatomia_guia} muestra un caso real que contiene una guía docente en crudo, nada más extraída de la página web y cómo se limpia para obtener únicamente lo importante para el sistema. A la izquierda se encuentra la guía docente con sus apartado y a la derecha lo que se almacena de todo ello, siendo únicamente dos de los diecisete rótulos los que pasan la limpieza y el resto se descarta.

\input{diagramas/DiagramaAnatomiaGuia.tex}



\subsection{Extracción de las salidas profesionales}

Las salidas profesionales se extraen como un bloque de texto por titulación. El catálogo produce 8 bloques de salidas,
uno por cada uno de los siete grados simples. En el caso de los dobles grados, solo hay uno que publica sus propias salidas profesionales (el cual se extrae), los otros 4 se consideran sus salidas como la concatenación de los grados simples.


  La Figura~\ref{fig:estructura_salidas} muestra la estructura de este tipo de elemento a partir de un caso real (las salidas profesionales del Grado en Ingeniería Informática). Consta de un solo bloque de texto continuo y se indexa únicamente con el nombre de la titulación.

\input{diagramas/DiagramaEstructuraSalidas.tex}

\subsection[Deduplicación de la colección]{Deduplicación de la colección}
\label{subsec:deduplicacion}

Durante el análisis del corpus recopilado se detectó una notable redundancia estructural en la fuente oficial. Las asignaturas de los primeros cursos de las carreras de ingeniería (como \textit{Física} o \textit{Matemáticas I}) comparten una guía docente idéntica.

Esta duplicidad afecta negativamente a la fase de recuperación del sistema RAG ya que varios fragmentos exactos pelearían por un puesto limitado del \textit{ranking} ($Top\text{-}K$). Para solventarlo, se implementó un proceso de eliminación de duplicidades previo a la fragmentación, conservando un único documento por grupo y consolidando en listas paralelas los identificadores y grados donde se imparte (\texttt{grados} y \texttt{codigos}).

\subsubsection{Criterio de identidad y selección de la clave (ADR-0001)}
\label{secc:criterio_identidad_deduplicacion}

Eliminar estas duplicidades exige definir cuándo dos guías docentes representan la misma entidad académica. Se evaluaron dos estrategias de agrupación, cuyos resultados se resumen en la Tabla~\ref{tab:claves_dedup}:

\begin{enumerate}
  \item \textbf{Agrupación exclusiva por contenido:} Asume que dos guías son iguales si sus textos coinciden exactamente.
  \item \textbf{Agrupación por par (nombre, contenido):} Exige que coincidan tanto la denominación de la asignatura como el cuerpo de la guía.
\end{enumerate}

La opción elegida fue la segunda, el par \texttt{(nombre, contenido)}. Lo que llevó a esta elección fue la detección de los casos reales en la web de la EPSJ donde asignaturas completamente distintas comparten por error el mismo texto de respaldo. Este fenómeno ocurre por ejemplo entre \textit{Seguridad en gestión de la información} (Grado en Inteligencia Artificial y Ciberseguridad) y \textit{Seguridad en tecnologías de la información} (Grado en Ingeniería Informática). Agrupar solo por contenido habría fusionado ambas materias en una sola unidad, provocando que una consulta sobre una titulación devolviera el temario de otra.

\input{tablas/TablaClavesDeduplicacion.tex}


\subsection[Verificación y reproducibilidad de la colección]{Verificación y reproducibilidad de la colección}
\label{secc:verificacion_fase0}

La fiabilidad de la Fase 0 descansa en dos verificadores ejecutados en el conjunto de datos completo. La primera es \texttt{check\_dataset.py}, que comprueba la integridad de grados, asignaturas y guías docentes (número de entidades, la presencia de campos obligatorios y tipos de asignaturas). El segundo, \texttt{check\_chunks.py}, valida los fragmentos y la deduplicación. Ambos se ejecutan en local antes de subir cambios, mientras que la integración continua ejecuta las pruebas unitarias (\texttt{pytest}) y la comprobación de tipos (\texttt{mypy}).

Las pruebas unitarias se emplean en base a los ficheros reales descargados llamados \textit{fixtures} de la web de la EPSJ. Es una decisión deliberada ya que los datos sintéticos no reproducen las anomalías reales de la fuente. Cada defecto encontrado se incorpora como caso de regresión con su HTML real.

Para garantizar la trazabilidad del conjunto de datos, el rastreador web guarda automáticamente la fecha en la que realizó la extracción y cada guía docente incluye su curso académico, el cual se extrae de la propia URL de la ficha en el catálogo de la Universidad. 

El conjunto de datos no se incluye en el repositorio en GitHub debido al tamaño actual y al que pudiera obtener en un futuro, la reproducibilidad se apoya en la capacidad de regenerarlo cuando sea y en el equipo que sea con el propio flujo de procesamiento del trabajo. Por ello, el rastreador y el fragmentador son módulos independientes, ya que el primero es lento y debe de dosificarse para no sobrecargar el servidor de la universidad, mientras que el segundo es rápido y se repite con frecuencia en los diferentes experimentos del proyecto.


\section[Fase 1: Fragmentación e Incrustaciones]{Fase 1: Fragmentación e Incrustaciones}
\label{secc:fase1}

Con la colección de datos ya recuperada, esta fase resuelve el problema de poder buscar en ella por el significado de las unidades extraídas y no por simple coincidencia de palabras que se podría hacer con un \texttt{grep}. Este procedimiento exige, pues, convertir cada texto extraído en un vector, y antes de eso, decidir dos cosas de este proceso, en qué trozos se parte la colección (y cómo), y con qué modelo se convierten esos trozos en vectores. Ambas se probarán de manera experimental.

Primero se introducirá el instrumento con el que se mide, que es el conjunto de preguntas creadas, después el troceado, con el experimento que fija sus parámetros, y por último el modelo de incrustaciones.


\subsection[El conjunto de evaluación como instrumento de medida]{El conjunto de evaluación como instrumento de medida}
\label{secc:conjunto_evaluacion}

Para la experimentación se ha usado el conjunto de datos extraídos y 66 preguntas creadas para abarcar todo tipo de cuestiones que el usuario promedio de
la aplicación podría querer saber.

Las preguntas abarcan seis tipos de cuestiones: el temario de una asignatura en concreto, listado de asignaturas de una
titulación, salidas profesionales de un grado, metadatos de asignaturas (cuántos créditos tiene una asignatura y de qué tipo es) y asignaturas sin guía publicada (para comprobar si alucina o solo la nombra), y un sexto tipo que no pregunta por esta institución. La Tabla~\ref{tab:conjunto_evaluacion} recoge el reparto, con un ejemplo literal de cada tipo.

\input{tablas/TablaConjuntoEvaluacion.tex}

Cada pregunta lleva anotada la unidad o unidades que el sistema debería recuperar para poder contestar correctamente a la pregunta.

\subsection[Troceado de la colección]{Troceado de la colección}
\label{subsec:chunking}

Los datos o corpus se tienen que trocear debido a la ventana de contexto explicada en el apartado~\ref{subsec:chunking_concepto}. El cómo se trocean y la cantidad en cada trozo es lo que se va a decidir en este apartado.

\subsubsection[Restricciones del troceado]{Restricciones del troceado}
\label{secc:troceado_restricciones}

El cuerpo de una guía docente ocupa una mediana de 2.653 caracteres y algunas de ellas llegan hasta los 24.047 caracteres, de modo que el 99\,\% de las guías no caben en un fragmento de 900 caracteres y el 83\,\% tampoco en uno de 1.500. Si se dejaran tal cual, el algoritmo de indexación obligaría al modelo a recortar la mayoría de estas perdiendo información en el camino.

A esa restricción de tamaño se suma una restricción de diseño que el sistema no puede saltar,
un fragmento nunca puede mezclar contenido de dos asignaturas distintas, porque un fragmento así recuperado atribuiría a una asignatura el temario de otra.

Con esas dos condiciones se compararon cuatro estrategias, recogidas en la Tabla~\ref{tab:alternativas_chunking}. Esta misma tabla dice el nombre de la estrategia y la descripción de lo que haría el algoritmo. La cuarta estrategia se nombra como una que se pensó en un inicio, pero por los argumentos dados anteriormente se descarta sin llegar a medirla, las otras tres pasan por el experimento. El registro de decisión de arquitectura ADR-0001\cite{repo_tfg} recoge el
  experimento completo, con sus cuarenta y cinco configuraciones.

\input{tablas/TablaAlternativasChunking.tex}

Las otras tres opciones no se sabe solamente leyendo su descripción cuál es mejor que otra, así que
se miden. Lo que sigue es el diseño de esa comparación; sus cifras, por la estructura que exige la memoria, están en el Capítulo~\ref{cap:resultados}.


\subsubsection[Experimento en rejilla]{Experimento en rejilla}
\label{secc:rejilla_fragmentacion}

La comparación de metodologías de troceado se organiza como una búsqueda en rejilla, es decir, se probaron sistemáticamente todas las combinaciones de los parámetros de las tres estrategias. Las tres estrategias elegidas para el experimento tienen la misma escala de tamaños máximos (600, 900, 1200, 1500 y 1800 caracteres) y sobre cada tamaño, cada una aporta tres variantes de su parámetro propio: el tamaño objetivo en la estructural (a veces igualado con el máximo), el percentil de corte en la semántica y el solape en la de tamaño fijo. Son cuarenta y cinco configuraciones en total, quince por estrategia.

Las tres estrategias y sus diferentes configuraciones trabajan bajo los mismos datos de entrada (las guías de cada asignatura y el bloque de salidas de cada titulación), y lo único que hacen es decidir cómo cortar los datos.

\subsubsection[Procedimiento seguido en cada configuración]{Procedimiento seguido en cada configuración}
\label{secc:rejilla_procedimiento}

El procedimiento seguido en este experimento en cada una de las cuarenta y cinco configuraciones es el mismo, se parte de la colección recogida con el rastreador web, y cada configuración tiene que trocear la colección entera desde el principio, para luego evaluar cómo lo hace con las preguntas del conjunto de evaluación.

El modelo de incrustaciones elegido en el ADR-0003, el cual tiene su propio apartado en la Sección~\ref{secc:resultados_embeddings}, se carga una sola vez para toda la
ejecución, y de él se toman dos funciones distintas: la primera convierte los trozos en vectores y la segunda es su analizador léxico, que se utiliza para saber cuántos fragmentos superan la ventana de contexto. 

El recorrido del experimento se organiza por tamaño máximo, de menor a mayor. Antes de generar las diferentes configuraciones de cada tamaño en específico, se trocea una vez la colección con la estrategia de referencia (que en este caso es la estrategia estructural por unidad semántica), esto se hace para medir la distribución de distancias entre las incrustaciones de piezas consecutivas del texto. De esta distribución salen los tres umbrales de la estrategia semántica. Se ha elegido usar percentiles en vez de distancias absolutas debido a que estos se adaptan a la distribución creada por ese modelo en específico, y se recalcula para cada tamaño máximo que en las siguientes iteraciones cambiará.

Con los umbrales ya calculados de la estrategia de referencia, se generan las nueve configuraciones diferentes, tres tamaños objetivo distintos $n$ para la estrategia estructural, tres solapes para la estrategia de longitud y tres percentiles para la semántica. Dentro de cada una se sustituye únicamente la función que decide dónde colocar los cortes dentro de una unidad y se ejecuta el código. 

De cada configuración se registran las métricas para cada uno, tres, cinco, diez y quince resultados (valores de $k$), el número de fragmentos generados, su tamaño mediano y cuántos de ellos superan la ventana del modelo y se truncan por ello. Recogidos todos los valores anteriores, se restaura el conjunto de datos original antes de pasar a la siguiente configuración para que ninguna iteración quede contaminada por la anterior.

Cabe recalcar que hacer de nuevo el experimento tarda alrededor de una hora y es determinista, por lo que si se volviera a repetir una y otra vez sin cambiar nada, daría los mismos resultados.



\subsubsection[El algoritmo de troceado y sus parámetros]{El algoritmo de troceado y sus parámetros}
\label{secc:troceado_algoritmo}

La estrategia elegida como ganadora del experimento es la estructural (el resultado está en la
  Sección~\ref{secc:resultados_fragmentacion}), y funciona de la siguiente manera. 
  El algoritmo parte de la unidad completa y solo la divide cuando hace
falta, primero corta por párrafos y, si algún fragmento sigue superando el tamaño máximo, lo subdivide por frases,
por último fusiona con sus vecinos los fragmentos residuales que se han quedado demasiado cortos y reequilibra el par resultante.
A cada fragmento se le antepone un encabezado autocontenido con el nombre de la asignatura, su tipo, sus créditos ECTS y
la titulación, de forma que conserve el sentido cuando se recupere aislado del resto de la base de datos vectorial. El tamaño máximo es de 900 caracteres, encabezado incluido, y el mínimo de 200. El máximo es una restricción dura, mientras que el mínimo es solo una preferencia,
ya que una unidad más corta que ese umbral no se puede alargar.


La unión de fragmentos solo ocurre cuando la unión resultante cabe dentro del máximo establecido, si no, el algoritmo conserva el fragmento corto en lugar de forzar la unión, ya que superar el máximo de la ventana haría que el modelo de incrustaciones cortase el fragmento en silencio y se perdiera contenido sin que nada lo advirtiera.


\subsubsection[El proceso completo y la estructura del fragmento]{El proceso completo y la estructura del fragmento}
\label{secc:estructura_fragmento}

La Figura~\ref{fig:secuencia_chunking} detalla la secuencia global de procesamiento sobre la colección de datos. Previo a la división del texto, se aplica la deduplicación explicada en la Sección~\ref{subsec:deduplicacion}.

A partir de esta consolidación, el algoritmo procesa la información mediante tres flujos diferenciados según la naturaleza del dato:

\begin{enumerate}
  \item \textbf{Contenido textual extenso (guías docentes y salidas profesionales):} Se genera un encabezado contextual con los metadatos de la asignatura (denominación, tipo, créditos ECTS y titulaciones vinculadas). A continuación, el cuerpo se particiona mediante el algoritmo estructural hasta ajustarse al tamaño objetivo (900 caracteres). Si el último fragmento resultante es menor de 200 caracteres, se fusiona con su predecesor siempre que la unión no exceda el límite máximo de la ventana.
  \item  \textbf{Asignaturas sin guía docente publicada:} Cada una genera un fragmento informativo breve con sus datos básicos, sin requerir particionado.
  \item \textbf{Fragmentos sintéticos y agregados:} Para optimizar las respuestas antes consultas globales de listado o recuento, se generan bloques estructurados directamente a partir del catálogo.
\end{enumerate}

\input{diagramas/DiagramaChunking.tex}

La anatomía interna resultante de este proceso se ilustra en la Figura~\ref{fig:estructura_chunk}. La clave del diseño radica en la separación de responsabilidades, ya que únicamente el campo \texttt{texto} (que fusiona el encabezado generado y el cuerpo de la fuente) se transforma en vector semántico. Los campos restantes (\texttt{origen}, \texttt{grados}, \texttt{codigos}, \texttt{chunk\_index} y \texttt{total\_chunks}) acompañan al vector como metadatos estructurados.

\input{diagramas/DiagramaEstructuraChunk.tex}

\subsubsection[Fragmentos derivados de la colección]{Fragmentos derivados de la colección}
\label{secc:fragmentos_derivados}

A partir de la colección y en código, se ha decidido generar fragmentos derivados de la colección para ayudar al modelo de generación a responder unos tipos específicos de preguntas.

Estas preguntas son del tipo de conteo como <<¿Cuántas asignaturas tiene el Grado en Ingeniería Informática?>> o de agregación como <<¿Qué asignaturas optativas tiene el Grado en Ingeniería Informática?>>. Estas harían que se recogieran muchos fragmentos, incluso más que el $k$ establecido, degradando en gran medida la respuesta final.

Sin embargo, si estas respuestas que se pueden calcular fácilmente se hacen previo a la indexación de la colección, se evita este problema y se hacen las respuestas más deterministas.

\subsection[Selección del modelo de incrustaciones]{Selección del modelo de incrustaciones}
\label{secc:fase1_embeddings}

Las cifras resultantes del experimento (los cuatro modelos, sus métricas y el desglose por tipo de pregunta) están en la Sección~\ref{secc:resultados_embeddings} del 
  Capítulo~\ref{cap:resultados}. Aquí se describe el diseño experimental y por qué se seleccionaron esos cuatro modelos y no otros.

\subsubsection{Diseño de la comparación}

La elección del modelo de incrustaciones o modelo de \textit{embedding} se resolvió de forma experimental y no únicamente por ficha técnica u opiniones en internet, comparando varios candidatos sobre los mismos fragmentos, los que produce la estrategia fijada en la Sección~\ref{secc:troceado_algoritmo} y sobre el conjunto de preguntas usadas también en el anterior experimento, lo único que varía de iteración a iteración es el modelo.

Se emplean dos medidas: la primera cuenta cuántos fragmentos han sido recuperados y describe la cobertura del sistema, y la segunda cuenta unidades (la asignatura completa, independientemente de en cuántos fragmentos se haya dividido). Si estos son muy dispares hay un problema, por ejemplo, si el sistema encuentra la asignatura correcta pero no todos sus fragmentos, el problema no está en la recuperación sino en el criterio con el que se delimitan las unidades.

Para la elección de los candidatos se buscó la mayor variedad posible, por lo que se eligió un modelo entrenado con español dado que toda la colección de datos es en español, y los otros tres multilingües, siendo uno de ellos más grande y de más capacidad que los otros de menor tamaño. Todos ellos a su vez comparten que la ventana de contexto es de al menos 512 para no tener que reducir la longitud de los fragmentos con los que se trabaja, también tienen una licencia compatible con la del repositorio y todos son ejecutables en el equipo donde se realizan los experimentos.


\subsubsection{Elección final}
\label{secc:eleccion_e5small}

De los cuatro candidatos, los dos que dieron mejores resultados pertenecen a la misma familia de modelos y se diferencian solamente en el tamaño, con casi cinco veces más parámetros el modelo mayor. Si se acude a los resultados del Apartado~\ref{secc:resultados_embeddings}, se observa que es el modelo más grande el que es más eficaz y arroja mejores resultados, sin embargo, la pregunta es otra: ¿se puede justificar la mejoría con el gasto en procesamiento extra que conlleva?

Al llevar a cabo los experimentos, se pudo apreciar que el modelo grande tarda bastante más (Apartado~\ref{secc:resultados_embeddings}) en incrustar la colección completa, aunque esto no sea tan grave dado que este proceso de indexación solo se realiza cuando cambia la colección de datos. El coste que sí importa mayormente sería el que hay que pagar cuando el modelo está en ejecución, ya que esto en el caso de este proyecto es grave puesto que tendrá que convivir en memoria principal junto con el LLM pudiendo llegar incluso a fallar por falta de espacio.

Se elige por tanto de los dos mejores modelos, el modelo pequeño (multilingual-e5-small), porque aunque no sea la opción más potente, dadas las circunstancias del trabajo es la opción más viable.
                                 
  

\section[Fase 2: base de datos vectorial, LLM y \textit{pipeline} RAG]{Fase 2: base de datos vectorial, LLM y \textit{pipeline} RAG}
\label{secc:fase2}


La fase 1 termina con la colección convertida en vectores. Lo que falta para que el sistema funcione correctamente son tres piezas, y esta fase las resuelve. La primera es dónde se guardan los vectores y con qué se consultan, que es lo que decide la comparativa de bases de datos vectoriales del apartado siguiente. La segunda es qué modelo redacta la respuesta una vez recuperados los fragmentos que hacen falta y la última pieza es el \textit{pipeline} RAG que une las dos anteriores.




\subsection[Selección de la base de datos vectorial]{Selección de la base de datos vectorial}
\label{secc:fase2_vectordb}

Este apartado recoge el diseño experimental, el cual conlleva hablar de qué se compara, con qué métricas, por qué estas tres tecnologías y con qué umbrales se juzgan. Todas las cifras resultantes de esta comparación se representan en la Sección~\ref{secc:resultados_vectordb} del Capítulo~\ref{cap:resultados}.


\subsubsection[Diseño de la comparación]{Diseño de la comparación}

Se comparan tres bases de datos vectoriales sobre el corpus completo y las preguntas del conjunto de evaluación, del cual ya se ha hablado anteriormente en esta memoria. A estas se le añadió una extra, la cual es la fuerza bruta, implementada con operaciones de álgebra matricial sobre la matriz de vectores. Esta última, la cual es NumPy, sirve para dos cosas, ser la referencia con la que se comprueba si una base pierde vecinos y por ello contexto para la respuesta, y ser la línea base de velocidad, dado que con los datos de este trabajo, es la opción más rápida.

Para este experimento, se cogen las incrustaciones que se generan mediante el modelo elegido anteriormente para este propósito y se dan a cada candidata en el mismo orden para que todas tengan la misma entrada. El procedimiento de medida elimina las primeras consultas de cada candidata, puesto que estas tardan más de lo normal al tener que meter el índice en memoria y contaminaría la mediana. Además, se repite cada consulta varias veces para quedarse con la mediana y el percentil 90 en lugar de con un único cronometraje.

\subsubsection[Las candidatas y por qué esas]{Las candidatas y por qué esas}

Las tres candidatas tienen una licencia que casa con la de este proyecto, que es la GPL versión 3\cite{gpl3} y cubren tres maneras diferentes de resolver el mismo problema. ChromaDB se ejecuta directamente dentro del mismo proceso de Python de la aplicación, para la persistencia combina una base de datos SQLite para los metadatos y carpetas binarias locales para los índices vectoriales, LanceDB utiliza un motor de almacenamiento persistente optimizado para datos tabulares y vectoriales y sus búsquedas y filtrados los realiza mediante DataFusion, y por último Qdrant se despliega como un servidor independiente ejecutándose en un contenedor Docker, con una comunicación cliente-servidor mediante APIs REST.

Se barajaron otras opciones pero se descartaron por estar en un estado de desarrollo muy prematuro, o por estar pensadas para corpus enormes.



\subsubsection[Los umbrales fijados]{Los umbrales fijados}




Antes de realizar el experimento, se pusieron ocho umbrales etiquetados como UX, siendo X el número del umbral. Estos se agrupan en tres clases:

\begin{itemize}
    \item \textbf{Eliminatorios : } son condiciones que hay que cumplir sí o sí como la fidelidad exacta respecto a la búsqueda por fuerza bruta, filtrado de metadatos sin ningún falso positivo, una cota superior de latencia que no se puede superar y otra cota de memoria residente que no puede sobrepasar.
    \item \textbf{Comparativos : } contemplan un margen por debajo del cual dos candidatas se declaran equivalentes.
    \item \textbf{Desempate : } Se resuelve en hechos verificables como por ejemplo si hace falta levantar un servicio aparte, si hay que declarar un esquema, cuántos índices de apoyo hay que crear, es decir, se elige por simplicidad.
\end{itemize}


\subsubsection[Elección final]{Elección final}
\label{secc:eleccion_lancedb}

De las tres candidatas, ChromaDB no alcanza la fidelidad exacta que exige el primer umbral ya que pierde fragmentos de cara a la respuesta en una parte de las reconstrucciones del índice (Apartado~\ref{secc:resultados_vectordb}). A una escala superior, esta pérdida sería minúscula y se compensaría con la velocidad de respuesta, pero a esta escala minúscula de datos, este error es fatal.

Entre las otras dos opciones que quedan de pie, no se decide únicamente por rendimiento. Qdrant responde algo más rápido que LanceDB, pero la diferencia es de milisegundos, luego LanceDB gana en construcción a Qdrant pero como ambos sobrepasan el umbral, no cabe fijarse en esto para establecer la decisión.

Lo que decide la tecnología es el desempate por simplicidad, ya que Qdrant necesita un servicio aparte en un contenedor y LanceDB se ejecuta dentro del propio proceso. La aplicación web pasaría de tener una arquitectura monolítica a depender de que el mismo equipo u otro levante un contenedor. También ayuda que LanceDB es la opción que menos pesa de las tres.

La ganadora del experimento es por tanto \textbf{LanceDB}, pero hay que decir que ninguna candidata gana a la búsqueda exacta por fuerza bruta y es que en este caso la colección de datos es pequeña, pero si escaláramos estos datos a toda la universidad, como la complejidad de este método es lineal, dejaría de ser efectiva, y es precisamente con este enfoque con el que se aborda este caso, obtener una herramienta ahora para el día de mañana aumentar los datos sin tener que tocar esta herramienta.

La Figura~\ref{fig:comparativa_vectordb} resume el recorrido de la comparación que muestra qué se midió en cada candidata y qué umbral decidió el descarte.

\input{diagramas/DiagramaComparativaVectorDB.tex}

\subsection[Construcción del índice]{Construcción del índice}
\label{secc:fase2_construccion_indice}

El índice es una estructura de datos organizada y optimizada donde se almacena la colección de fragmentos generada por la fase anterior, del cual también se descarta el registro de procedencia que encabeza el fichero ya que este describe de cuándo es el corpus y no es contenido realmente útil para el sistema. El índice actúa como la memoria del sistema de este proyecto, de modo que cada fragmento de la colección queda registrado de forma indisociable junto a su vector de incrustación y a sus metadatos asociados (titulaciones, códigos de asignatura y tipología). 

Este índice se reconstruye entero en cada ejecución en lugar de actualizarse de forma incremental, ya que reconstruirlo cuesta minutos y garantiza que el índice siempre coincida con la colección de fragmentos, mientras que hacer actualizaciones parciales obligaría a tener más factores en cuenta innecesarios a este coste de reconstrucción. El índice es un producto regenerable y derivado de la fuente de verdad que son los datos extraídos de la web de EPSJ.

Los tipos de datos de cada columna se establecen antes de dejar que la base de datos adivine los tipos, pudiendo poner uno que no sea el correcto. Esto se hace así dado que la base de datos deduce el tipo de cada columna a partir de los valores del primer lote de datos, resultando que si este primer lote viniera sin códigos de asignatura, la base podría poner un tipo equivocado pensando que nunca hay códigos numéricos ahí.

Las titulaciones y asignaturas se guardan como listas paralelas y no como una sola cadena de texto para garantizar un filtrado exacto. Esto evita que las búsquedas por subcadena arrastren fragmentos erróneos en grados cuyos nombres están contenidos dentro de otros más largos (como ocurre en el \texttt{Grado en Ingeniería eléctrica} frente al \texttt{Doble Grado con Mecánica}.) De este modo evitamos que se recojan más fragmentos de los que realmente es necesario para no contaminar el modelo generativo.


Por último, este índice registra tres datos importantes, como con qué modelo se calcularon las incrustaciones, con qué prefijo se marcaron los fragmentos incrustados y con qué métrica de distancia consultarlo (similitud del coseno o euclídea). Esto se hace para que el buscador pueda verificar automáticamente al empezar que se están usando los mismos parámetros con los que se creó el índice para evitar errores.



\subsection[La conversación con el asistente]{La conversación con el asistente y el problema con el contexto}
\label{secc:fase2_conversacion}

Un estudiante o un usuario objetivo de la aplicación no tiene un guion montado sobre qué decir, es decir, no preguntará cuestiones perfectamente escritas, sino que hará referencia a anteriores respuestas y cuestiones y seguramente tenga faltas de ortografía, haciendo que el sistema pudiera no entender completamente las consultas. Estas preguntas implícitas podrían ser tales como <<¿Y segundo?>>, que no dice en la misma pregunta de qué titulación está hablando, o <<¿Y el doble grado?>>, que tampoco hace referencia a la titulación de la que se tiene que buscar la doble titulación, sino que se tiene que sacar por contexto anterior.

Esto genera un problema y es el del contexto de la conversación y es un problema serio puesto que si no lo tuviéramos en cuenta y el agente solo se enfocase en la última pregunta se convertiría en una especie de base de datos a la que le tienes que hacer la consulta perfectamente para que te responda bien lo cual no es lo que se busca en este proyecto, sin embargo, si pones que almacene todo el contexto de la conversación, almacenará datos basura que ayudan en la respuesta y debido a la escasez de recursos no es una solución viable tampoco.




\subsubsection{Estrategia de seguimiento del sujeto y resolución de anáforas}
\label{secc:fase2_gestion_contexto}

Para resolver este problema se diseñó un mecanismo determinista de seguimiento de estado en el que se desacopla la recuperación semántica del historial completo. El sistema gestiona el contexto mediante dos principios:

\begin{enumerate}
  \item \textbf{Seguimiento y persistencia del sujeto activo:} El modelo monitoriza en cada turno la titulación mencionada. Si el usuario nombra un nuevo grado, el sujeto (el grado) se actualiza, si formula una pregunta del grado anteriormente descrito, el sistema hereda automáticamente la titulación del turno anterior. Además, si la titulación fue propuesta por el propio asistente en la última respuesta (por ejemplo, recomendando el Grado en Ingeniería Informática ante un interés por el desarrollo de software), esta se coge como sujeto activo.
  \item \textbf{Ventana acotada de diálogo:} En el \textit{prompt} final hacia el LLM se incorporan únicamente los tres últimos turnos del usuario junto con el sujeto contextualizado, evitando la saturación del contexto.
\end{enumerate}

La Figura~\ref{fig:conversacion_tres_turnos} ilustra la dinámica temporal de este mecanismo sobre un diálogo real de tres turnos.
\input{diagramas/DiagramaConversacion.tex}

Las tres estrategias (incrustar la pregunta sola, concatenar la
  anterior y deducir el sujeto) se midieron sobre 39 conversaciones derivadas de la
  propia colección. \sout{Las cifras están en el apartado de resultados que comparaba las tres.}\textcolor{borrador2}{Las cifras están en el informe del experimento que
  recoge el repositorio\cite{repo_tfg}.}



\subsection[Selección del modelo de generación]{Selección del modelo de generación}
\label{secc:fase2_llm}

  Ahora que se han extraído los datos, y se han fragmentado, el siguiente paso es que un modelo de inteligencia artificial (modelo de lenguaje)
  utilice estos datos para elaborar la respuesta que satisfaga las dudas del usuario. En este apartado se hablará de este modelo que va a realizar esta acción.

\subsubsection{Por qué la comparación no es tan objetiva como en ocasiones anteriores}

En el caso de las comparativas y experimentos anteriores (incrustaciones y base de datos vectorial), se decidían por motivos claros, opciones mejores que otras u opciones que por el alcance y enfoque de este proyecto, son elecciones mejores. Sin embargo se han buscado objetivos claros que acerquen a un solo modelo a la elección, como que no alucine o que teniendo los fragmentos delante, los lea y genere la respuesta con todos ellos.


Sobre cada respuesta lanzada por el modelo que se está midiendo, se calculan cuatro medidas, todas ellas comparaciones de cadena contra la colección de datos:

\begin{itemize}
  \item \textbf{Titulaciones inventadas: } Nombres de titulaciones que si bien tienen sentido que se den en una escuela Politécnica superior, no se encuentra en los datos recogidos.
  \item \textbf{Precisión: }Mide qué parte de la respuesta generada existe de verdad en la colección.
  \item \textbf{Cobertura: }Qué proporción de lo que debía decir realmente aparece.
  \item \textbf{Acierto escalar: }Se aplica únicamente a las preguntas cuyas respuestas es un único valor como los créditos de una asignatura o el número de asignaturas optativas de una titulación.
\end{itemize}

Cabe destacar que las cifras calculadas de estas cuatro medidas están en el Apartado~\ref{secc:resultados_rag}.


Para evitar problemas como falsos positivos o penalizaciones en las respuestas del agente que son cadenas de caracteres, se establecen tres criterios tales como exigir la unidad junto al número de créditos (que ponga 6 créditos o 6 ECTS) para evitar números al azar, admitir los nombres de la titulación con o sin el prefijo <<Grado en>>, y aceptar cualquiera de los cursos válidos en asignaturas de itinerario variable en dobles grados.

\subsubsection{El banco de preguntas elegido}

Para evaluar y comparar el rendimiento de los modelos de forma objetiva y reproducible, se construyó un banco de evaluación generado automáticamente mediante código a partir de la colección de datos oficial. Este enfoque deriva tanto las preguntas como sus respuestas correctas (\textit{ground truth}) directamente del conjunto de datos sin requerir etiquetado manual como si pasaba anteriormente, garantizando que el banco crezca de forma escalable si la fuente se amplía en el futuro.

El banco de preguntas en su totalidad contiene una cantidad enorme de preguntas, haciendo que si se quisieran probar todas supondría más de 4.000 inferencias y un gran número de horas en un equipo como con el que se está realizando este proyecto. La comparativa en la práctica se realizó con una muestra representativa de 80 preguntas, reservando el banco completo para la validación final del modelo ganador. Esta extracción de las 80 preguntas se llevó a cabo mediante un muestreo estratificado con una semilla fija común para todos los modelos, garantizando la paridad y la reproducibilidad de las pruebas.

El banco se estructura en seis familias temáticas representativas del usuario objetivo, tales como créditos de asignaturas, curso de impartición, planes de estudio completos, asignaturas de mención, asignaturas optativas y catálogo general de la Escuela. Este banco no está equilibrado, haciendo que una familia tenga una gran cantidad de preguntas mientras que otras tengan unas pocas, esto se arregla reequilibrando intencionadamente esta proporción en la prueba. 


\subsubsection{El umbral del experimento}

Los umbrales del experimento son dos:

\begin{itemize}
  \item \textbf{Ninguna titulación inventada: }Es un umbral eliminatorio, lo que significa que si se llegase a inventar alguna, el modelo sería totalmente descartado.
  \item \textbf{Ningún candidato se quede sin contexto: }Si a un modelo se le ofreciera menos fragmentos que los demás no se estarían comparando modelos, sino las recuperaciones que les tocaron.
\end{itemize}

Hay dos magnitudes que se miden del experimento pero no se tienen en cuenta para la decisión del modelo. La primera es el tiempo que este tarda en responder ya que no todos los modelos se comprueban bajo las mismas condiciones controladas, y el equipo en el que se hacen es un portátil de desarrollo que tiene más aplicaciones abiertas. 

La otra magnitud es el reparto de uso entre la tarjeta gráfica y el procesador. Al seleccionar un modelo más capaz y por ende más grande, se excede la memoria de la tarjeta gráfica disponible y parte de su ejecución se traslada a la memoria principal, por lo que los tiempos medidos reflejan las limitaciones del hardware de desarrollo del proyecto más que la eficiencia intrínseca de cada modelo. Ambas magnitudes se documentan y analizan como una amenaza a la validez en el Apartado~\ref{secc:amenazas_validez}.

\subsubsection[Elección final]{Elección final}
\label{secc:eleccion_gemma3}

Los cuatro candidatos finalmente elegidos después de una criba de múltiples modelos, superaron los umbrales eliminatorios, ninguno inventó titulaciones inexistentes  y todos dispusieron del contexto necesario. Al no existir descartes directos, la decisión se fundamentó en las métricas de rendimiento del Apartado~\ref{secc:resultados_rag}.

A partir de los resultados del experimento se adopta \texttt{gemma3:12b}, al ser el único modelo que no queda por detrás en ninguna métrica, sin embargo, \texttt{ministral-8b} y \texttt{qwen3.5:9b} obtienen valores menores en cobertura y acierto escalar, mientras que el modelo español, \texttt{salamandra-7b} queda más rezagado.

Lo peor del mejor modelo es su tiempo mediano de respuesta, que es el más alto de los tres finalistas (Tabla~\ref{tab:comparativa_generacion}). Esto tiene una explicación clara y es que al ser un modelo más grande y capaz, consume mucho más recursos y tarda más. Esta diferencia se asumen conscientemente conforme a la jerarquía del requisito RNF-04 (Tabla~\ref{tab:requisitos_no_funcionales}), que prioriza la fidelidad factual y la ausencia de alucinaciones sobre la velocidad de inferencia.

Asimismo, tal como se justificó en el apartado anterior, penalizar al modelo por su latencia, mediría las restricciones de memoria del hardware de desarrollo y no la capacidad intrínseca de la arquitectura (ADR-0005\cite{repo_tfg}), ya que gran parte del modelo debe ejecutarse en CPU al superar la capacidad de la tarjeta gráfica.



\subsection[El \textit{pipeline} RAG completo]{El \textit{pipeline} RAG completo}
\label{secc:fase2_pipeline}

Con la colección de datos extraída, troceada e indexada y el modelo generativo elegido, este apartado describe el recorrido completo de qué se recupera, qué se le entrega al modelo y qué se hace cuando no hay nada que entregarle. Los fundamentos teóricos de la recuperación semántica y del paradigma \gl{RAG} se introdujeron en el Capítulo~\ref{cap:antecedentes}
  (Apartado~\ref{subsec:busqueda_semantica_concepto}), aquí se detalla su implementación concreta en la arquitectura del asistente.

  La Figura~\ref{fig:secuencia_rag} resume el ciclo de vida mediante un diagrama de secuencia. El aspecto central de este flujo reside en los tres desvíos representados con trazo discontinuo, que constituyen las vías de salida anticipada sin que llegue siquiera a llamar al modelo, ahorrando mucha capacidad de cómputo.


\input{diagramas/DiagramaSecuenciaRAG.tex}

\subsubsection[Recuperación adaptativa y filtrado de fragmentos]{Recuperación adaptativa y filtrado de fragmentos}

El número de fragmentos que se le entregan al modelo ($k$) no es fijo, sino que varía según el tipo de pregunta que se haga, haciendo que si solo se necesita unos pocos fragmentos traiga esos y no más para no ensuciar la respuesta, mientras que si necesita una gran cantidad de fragmentos para construirla, lo haga sin problemas, siempre poniendo un límite.

Para plantear esta solución, el sistema utiliza una política de recuperación dinámica que combina tres mecanismos complementarios:

\begin{itemize}
  \item \textbf{Banda de seguridad:} Acota el número de fragmentos a un rango admisible, garantizando un mínimo de 3 y un máximo de 20 fragmentos por consulta.
  \item \textbf{Corte relativo por distancia:} Dentro de dicha banda, se conservan únicamente aquellos fragmentos cuya distancia semántica no supere un factor proporcional respecto al mejor fragmento recuperado. De este modo, las consultas con una respuesta muy unívoca retienen muy pocos elementos, mientras que las de listado arrastran a todo su grupo semántico.
  \item \textbf{Suelo absoluto de pertinencia:} Establece un umbral de similitud mínima por debajo del cual no se entrega ningún fragmento, descartando información de baja calidad incluso si constituye el mejor resultado disponible.
\end{itemize}


Cuando ningún fragmento supera el suelo de pertinencia, el sistema devuelve una respuesta fija sin llegar siquiera a invocar al modelo para que razone, eliminando por completo la posibilidad de alucinación.



\subsubsection{Estructura y composición del \textit{prompt}}

La instrucción de entrada que el generador ensambla y transmite al modelo de lenguaje se divide en dos niveles de comunicación:

\begin{enumerate}
  \item \textbf{Mensaje de sistema (\texttt{system}):} Se transmite en un campo independiente del cuerpo de la consulta. Define el rol del asistente como orientador académico de la EPSJ, estableciendo un tono institucional, formal y pedagógico adaptado a estudiantes preuniversitarios.
  \item \textbf{Mensaje de usuario (\texttt{prompt}):} Concatena de forma estructurada los componentes de instrucción y datos:
  \begin{itemize}
    \item \textbf{Instrucciones operativas fijas:} Directivas estrictas que obligan a responder únicamente con la evidencia del contexto, declarar la falta de información cuando proceda y citar la procedencia de cada dato.
    \item \textbf{Oferta oficial del centro:} Relación explícita de las doce titulaciones de la Escuela.
    \item \textbf{Ámbito conversacional:} Titulación activa deducida en el turno actual para contextualizar la búsqueda.
    \item \textbf{Historial de preguntas del usuario:} Las tres últimas consultas planteadas por el estudiante, utilizadas exclusivamente para resolver dependencias anafóricas.
    \item \textbf{Contexto documental:} Bloques textuales recuperados por LanceDB que superan el umbral de pertinencia, acompañados de sus encabezados identificativos.
    \item \textbf{Pregunta actual:} Consulta del usuario delimitada formalmente para señalar el inicio de la generación.
  \end{itemize}
\end{enumerate}

La Figura~\ref{fig:prompt_despiezado} despieza esa construcción y señala qué parte del mensaje aporta cada componente.

\input{diagramas/DiagramaPromptDespiezado.tex}


El mensaje de sistema viaja desacoplado para impedir que el modelo lo confunda con datos documentales. Asimismo, la inclusión exclusiva de las preguntas del usuario y no de las respuestas generadas por el asistente previene la retroalimentación de fallos por parte de este.

\subsection[Restricción del dominio]{Restricción del dominio}
\label{secc:fase2_dominio}

El objetivo del asistente es el de informar sobre la oferta de la Escuela y no debería irse de este tema. La necesidad de acotar el dominio radica en el riesgo de que el modelo recomiende titulaciones inexistentes o ajenas al centro al intentar dar una respuesta coherente. Para evitar que el sistema induzca a error a futuros estudiantes universitarios, se implementó una estrategia de restricción articulada en tres capas complementarias.

La primera capa es el suelo de pertinencia explicado en el apartado anterior. Si bien este umbral descarta una parte considerable de las consultas ajenas al dominio, no constituye un filtro completo que abarque todos los casos. Aquellas preguntas que emplean vocabulario formal o universitario sobre estudios externos (como preguntar si se puede estudiar medicina en el centro o por los planes de estudio de otras facultades) presentan vectores semánticamente muy próximos al corpus indexado, dejando pasar este tipo de consultas al compartir vocabulario con el centro.

Para cubrir las consultas que superan el suelo de pertinencia, la segunda capa actúa antes de la llamada al modelo mediante un filtro de centros externos. El sistema comprueba si se hace referencia a otra institución mediante patrones léxicos y si se encuentra esta opción, la consulta se detiene incluso antes de llamar al modelo.


La tercera capa opera después de generar la respuesta como una validación del catálogo oficial. Tras recibir el resultado de la inferencia redactada por el modelo, un extractor basado en expresiones regulares identifica todas las palabras con formato de titulación y las compara contra las doce titulaciones oficiales de la Escuela. Si se detecta cualquier titulación ajena o inventada, el sistema descarta la respuesta íntegra y entrega un mensaje informativo prefijado.

las tres capas mecánicas actúan de forma secuencial y determinista, asegurando que cada una capture las anomalías que escapan a las demás. La distribución cuantitativa del rechazo obtenido por cada mecanismo se analiza en detalle en el Apartado~\ref{secc:resultados_rag_sistema}.




\section[Fase 3: la aplicación web]{Fase 3: la aplicación web}
\label{secc:fase3}

\subsection[Arquitectura de la aplicación]{Arquitectura de la aplicación}
\label{secc:fase3_arquitectura}

La aplicación funciona con un solo proceso de servidor que atiende las preguntas y sirve la interfaz, el cual importa como biblioteca el flujo de procesamiento descrito en la sección \ref{secc:fase2_pipeline}. La Figura~\ref{fig:arquitectura_web} recoge la arquitectura, dejando fuera el servidor de inferencia.


\input{diagramas/DiagramaArquitecturaWeb.tex}





Para integrar el flujo de procesamiento en el servidor web existían dos opciones: ejecutarlo como un programa independiente en cada consulta o importarlo directamente como biblioteca dentro del servidor. Se eligió la segunda opción por dos motivos fundamentales:
\begin{itemize}
  \item \textbf{Latencia:} ejecutarlo de forma independiente obligaría a cargar el modelo de incrustaciones y el índice vectorial desde cero en cada pregunta, haciendo esperar al usuario varios segundos en cada mensaje. Al importarlo como biblioteca, ambos se cargan una sola vez al arrancar y quedan listos para responder de inmediato.
  \item \textbf{Gestión de memoria:} en un equipo doméstico donde el modelo generativo ya ocupa gran parte de la memoria disponible, crear procesos adicionales por cada petición competiría por los recursos restantes.
\end{itemize}

Una alternativa que se barajó fue repartir el sistema en microservicios (uno de recuperación, otro de generación y una pasarela). Se descartó porque esta arquitectura está pensada para escalar y desplegar componentes por separado en múltiples máquinas, problemas que este trabajo no presenta. Al ejecutarse todo en un único equipo, añadir procesos solo generaría sobrecarga de comunicación y un mayor consumo de memoria. Por ello, la única separación de procesos que se mantiene es la del servidor de inferencia, justificada para conservar el modelo generativo residente en memoria y evitar tiempos de recarga en cada consulta.

\subsection[El punto de entrada]{El punto de entrada}
\label{secc:fase3_punto_entrada}

Para atender las peticiones web y servir los archivos de la interfaz, el sistema utiliza directamente el módulo estándar \texttt{http.server} de Python, sin incorporar ningún marco ni tecnología externa. La decisión de mantener lo más simple es porque al medir el número de paquetes adicionales que cada una de las alternativas exigía instalar sobre el entorno base, era numeroso.

Otra razón es la simplicidad del servicio, la aplicación en el alcance establecido solo necesita gestionar un único punto de entrada para las consultas y servir unos pocos archivos estáticos (\sig{HTML}, \sig{CSS} y \textit{JavaScript}). Las ventajas que justifican el uso de marcos de trabajo más complejos no resuelven ningún problema presente en esta arquitectura.

En cuanto a las limitaciones técnicas de la tecnología usada, \texttt{http.server} no está concebido para entornos de producción, ya que procesa las solicitudes de forma secuencial, carece de soporte nativo para conexiones cifradas \sig{HTTPS} y la propia documentación oficial desaconseja su uso en producción. No obstante, estas restricciones no afectan al trabajo, ya que el despliegue multiusuario en producción se declaró fuera de alcance y el sistema está diseñado únicamente para ejecutarse en el equipo local. 

La lógica y verificación no reside en el manejador de peticiones \sig{HTTP}, sino en una función desacoplada que recibe la consulta y emite progresivamente fragmentos de texto ya comprobado. Esto asegura una fácil sustitución de \texttt{http.server} por un marco de trabajo más complejo cuando se escale y se amplíe el alcance del proyecto.

\subsection[La emisión de la respuesta]{La emisión de la respuesta}
\label{secc:fase3_emision}

El mecanismo el cual la respuesta generada se transmite al navegador es una decisión crítica de diseño condicionada por la latencia del modelo en generar la respuesta. Al medir las consultas del banco de evaluación del sistema, se observó que la espera se mide en decenas de segundos, y que una de cada diez consultas que llegan al modelo pasa de los dos minutos (Apartado~\ref{secc:resultados_rag_sistema}). Entregar la respuesta de una sola vez obligaría al usuario a esperar frente a una interfaz estática durante más de un minuto, si antes de eso no pensara que el sistema se ha bloqueado.

Una solución que se pensó en un inicio fue la emisión progresiva convencional, pero esto entra en conflicto directo con la tercera barrera de restricción del dominio (Apartado~\ref{secc:fase2_dominio}). Emitir el texto de forma continua a medida que el modelo genera cada palabra impediría eliminar una respuesta que incluya una titulación inventada que el usuario ya haya leído.

Para juntar ambos requisitos, el sistema implementa una emisión progresiva validada por bloques. El servidor acumula la salida del modelo hasta identificar el cierre de una frase (punto, interrogación o exclamación) o un salto de línea. En ese momento, somete el texto del bloque a la validación léxica del catálogo y, únicamente si la supera, lo envía a la interfaz. Si en algún bloque se detecta una titulación incorrecta, el servidor interrumpe la generación de inmediato, ordena a la interfaz retirar el texto emitido hasta ahora y entrega el mensaje fijo de advertencia.

Esta solución se asume aún siendo no lo mejor para la experiencia de usuario ya que este vería como un texto que se está generando de repente desaparece por un texto que dice que no ha podido generarle una respuesta. Sin embargo, la frecuencia de estas anomalías es muy baja siendo cero en las múltiples respuestas de la comparativa de modelos. La comparación de las tres alternativas consideradas queda documentada formalmente en el ADR-0006\cite{repo_tfg}.

\subsection[La interfaz]{La interfaz}
\label{secc:fase3_interfaz}

Dado el enfoque experimental del trabajo centrado en las tecnologías de generación aumentada por recuperación, se ha buscado que la interfaz sea lo más sencilla posible, siendo una única página servida por un único proceso y escrita en \sig{HTML}, hojas de estilo y \textit{JavaScript} sin marco de trabajo (\textit{framework}) ni paso de compilación previo. A la aplicación web se puede acceder sin registro, como ya se anunciaba en el requisito de usuario RU-05 de la Tabla~\ref{tab:requisitos_usuario}. Por tanto, no hay cuentas de usuario, ni sesiones persistentes, ni base de datos de usuarios que proteger.


\subsection[El guion gráfico de la pantalla]{El guion gráfico de la pantalla}
\label{secc:fase3_storyboard}

Antes de que se programara la interfaz web, se formalizó el diseño conceptual de la interacción mediante un guionb gráfico (\textit{storyboard}) que contempla tanto el caso en el que el asistente usa los fragmentos documentales recuperados, y el escenario en el que el corpus no contiene información suficiente para responder a la consulta (Figura~\ref{fig:storyboard_chat}).

\input{diagramas/DiagramaStoryboardChat.tex}

Una parte importante de la interfaz es el panel que muestra la fuentes documentales, este solo se muestra cuando existen fragmentos relevantes recuperados que respalden la afirmación de la repuesta. 

También se han tendido en cuenta tres estados de la conversación que la interfaz debe resolver para mantener la usabilidad, mostrados coomo estados en al Figura~\ref{fig:estados_chat}:

\begin{itemize}
  \item \textbf{La espera durante la inferencia:} La ejecución del modelo del lenguaje en el equipo local (RNF-01) y la prioridad de la fidelidad sobre la latencia (RNF-04) conlleva tiempos de inferencia más altos. Para evitar la sensación de bloqueo o congelamiento de la aplicación, la interfaz incorpora un indicador visual de la actividad con mensajes informativos sobre el estado actual de la respuesta (Apartado~\ref{secc:fase3_interaccion}).

  \item \textbf{La respuesta determinista inmediata:} Determinados mensajes como los saludos ("Hola", "buenos días", etc...) o cuando se detectan preguntas sobre centros ajenos a las EPSJ, se resuelven mediante respuestas prefijadas por reglas de control (RF-07). Al no requerir la inferencia del modelo, la respuesta la genera el código y es prácticamente instantánea y se muestra de forma directa en la conversación (Apartado~\ref{secc:fase2_dominio}).

  \item \textbf{La declaración de ausencia de información:} Cuando el ámbito de una pregunta excede el catálogo de información oficial disponible (como la solicitud de notas de corte o plazos de matrícula), el sistema informa expresamente de que les faltan estos datos para evitar alucinaciones (RU-03, RU-06). Se trata esto como en el caso anterior, como una respuesta válida, integrándola en el flujo de chat sin presentarla como un error.
\end{itemize}

\input{diagramas/DiagramaEstadosChat.tex}


\subsection[Accesibilidad y contraste visual]{Accesibilidad y contraste visual}
\label{secc:fase3_accesibilidad}

La interfaz se diseñó para cumplir con el nivel AA de las pautas de accesibilidad \sig{WCAG} (RNF-07). Los textos se contrastan con el fondo en valores que oscilan entre 4,92:1 y 14,83:1, superando el umbral mínimo exigido de 4,5:1. También, los bordes de los controles interactivos se ajustaron para garantizar un contraste de 3,02:1 frente al fondo, satisfaciendo el criterio 1.4.11 (mínimo de 3,0:1 para elementos de la interfaz) y asegurando su correcta percepción visual.


\subsection[Transparencia, sugerencias y control de espera]{Transparencia, sugerencias y control de espera}
\label{secc:fase3_interaccion}

El requisito RF-09 sobre trazabilidad pide mostrar junto a las respuestas las fuentes de la colección de las que sale. Esto se ha resuelto con una ventana emergente en lugar de un listado siempre visible para no saturar la interfaz de lectura (Figura~\ref{fig:app_fuentes}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.75\textwidth]{imagenes/app_fuentes.png}
 \caption[Cuadro de fuentes consultadas]{Ventana modal con el listado de fuentes documentales recuperadas del índice para la respuesta activa, agrupadas por titulación. Fuente: Elaboración propia.}

  \label{fig:app_fuentes}
\end{figure}

Bajo el área de conversación se ofrecen cuatro posibles preguntas sugeridas a modo de atajo por si no sabes qué preguntar. Esta idea surgió de las aplicaciones web de las grandes compañías de la IA, como ChatGPT o Google Gemini que contienen funcionalidades parecidas. El servidor las genera comprobando previamente contra el índice vectorial la existencia de los fragmentos que las respaldan.

El sistema genera 10 plantillas parametrizadas con más de noventa preguntas válidas sobre el corpus. De las cuatro sugerencias mostradas, dos se reservan a la titulación tratada en la conversación actual y dos a otras titulaciones del catálogo. 

Por último, la emisión progresiva por fases (Apartado~\ref{secc:fase3_emision}) mitiga la pantalla en blanco ante las grandes pausas entre las respuestas. Para evitar que el usuario asuma que la respuesta ha finalizado prematuramente, la interfaz mantiene un indicador de actividad continuo hasta recibir el cierre del flujo de inferencia.

La Figura~\ref{fig:app_inicio} recoge el estado inicial de la
  aplicación, con la cabecera institucional y las cuatro sugerencias que resuelven la
  pantalla vacía descrita más arriba.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{imagenes/app_inicio.png}
\caption[Pantalla inicial de la aplicación web]{Vista inicial del asistente con la cabecera institucional, el mensaje de bienvenida y el bloque de preguntas sugeridas. Fuente: Elaboración propia.}

  \label{fig:app_inicio}
\end{figure}

La Figura~\ref{fig:app_listado} muestra el caso de las preguntas de
  listado, donde la interfaz agrupa las asignaturas por curso en lugar de entregarlas
  en prosa seguida.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{imagenes/app_listado.png}
  \caption[Visualización de asignaturas por curso]{Ejemplo de respuesta del asistente estructurando el plan de estudios de una titulación organizado por cursos académicos. Fuente: Elaboración propia.}

  \label{fig:app_listado}
\end{figure}

Por último, la Figura~\ref{fig:app_movil} comprueba el requisito de
  adaptación a pantallas estrechas sobre un ancho de 390~píxeles.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.42\textwidth]{imagenes/app_movil.png}
  \caption[Adaptabilidad de la interfaz en dispositivos móviles]{Vista de la aplicación web adaptada a pantallas de dispositivos móviles (resolución de 390~píxeles de ancho). Fuente: Elaboración propia.}

  \label{fig:app_movil}
\end{figure}

\subsection[Registro de actividad para experimentación]{Registro de actividad para experimentación}
\label{secc:fase3_registro}

Durante la fase de pruebas y evaluación en el entorno local de desarrollo, el servidor almacena el registro de la conversación actual. Este registro se ha pensado únicamente para esta etapa y sirve para auditar las sesiones, en un despliegue en producción no se almacenarían conversaciones ni recursos de los usuarios.

